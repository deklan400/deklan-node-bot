# ==========================================
# ✅ TELEGRAM BOT (WAJIB)
# ==========================================
# Token bot Telegram
BOT_TOKEN=YOUR_BOT_TOKEN

# Chat ID admin utama
CHAT_ID=123456789

# (Opsional) User ID tambahan yang diizinkan, koma-separate. Contoh: 111,222,333
ALLOWED_USER_IDS=

# Kirim notif saat node UP/DOWN (1=ya, 0=tidak)
NOTIFY_UP=1
NOTIFY_DOWN=1


# ==========================================
# ✅ NODE SETTINGS
# ==========================================
# Nama systemd service RL-Swarm
SERVICE_NAME=gensyn

# Nama/label node (tampil di Telegram)
NODE_NAME=deklan-node

# Lokasi RL-Swarm (installer & bot akan normalisasi rl-swarm -> rl_swarm)
RL_DIR=/root/rl_swarm

# Folder identity keys (wajib berisi: swarm.pem, userApiKey.json, userData.json)
KEY_DIR=/root/deklan

# Jumlah baris log utk tombol LOGS
LOG_LINES=80

# Interval auto-monitor (menit) — default 180 (3 jam)
MONITOR_EVERY_MINUTES=180

# Pola log untuk info "round" (biar mudah ganti jika upstream berubah)
ROUND_GREP_PATTERN=Joining round:


# ==========================================
# ✅ AUTO INSTALLER REPO
# ==========================================
# Base URL untuk install/update/reinstall/uninstall script
AUTO_INSTALLER_GITHUB=https://raw.githubusercontent.com/deklan400/deklan-autoinstall/main/


# ==========================================
# ✅ SMART/RECOVERY OPTIONS (OPSIONAL)
# ==========================================
# Paksa git pull ketika repo RL_DIR terdeteksi git (1=paksa, 0=skip)
FORCE_GIT_PULL=1

# Rebuild image saat update/reinstall (1=ya, 0=tidak)
DOCKER_REBUILD=1

# Coba auto-reinstall dari monitor ketika restart gagal (1=ya, 0=tidak)
MONITOR_TRY_REINSTALL=1

# Maks karakter log yang dikirim Telegram saat FAIL (default 3500)
LOG_MAX_CHARS=3500


# ==========================================
# ✅ DANGER ZONE — ⚠
# ==========================================
# 0 = OFF (tombol Danger Zone disembunyikan), 1 = ON
ENABLE_DANGER_ZONE=0

# Password untuk aksi berbahaya. Kosong = nonaktif.
DANGER_PASS=
